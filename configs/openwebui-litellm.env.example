OPENAI_API_BASE_URL=http://host.docker.internal:4000/v1
OPENAI_API_KEY=replace-with-litellm-master-or-virtual-key

# Optional: set after LiteLLM /v1/models shows the route.
# DEFAULT_MODELS=tken-chat

# Keep the upstream TKEN key in the LiteLLM process, not in Open WebUI.
