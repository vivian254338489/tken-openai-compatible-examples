# Screenshot And GIF Brief

Use this file before publishing the GitHub repository or submitting a developer-community post.

## Static Screenshots

- `assets/preview-terminal-node.png`
  - Show `node node/chat.js` returning one successful assistant response.
  - Hide or mask API keys.
- `assets/preview-terminal-python.png`
  - Show `python python/chat.py` returning one successful assistant response.
  - Keep `OPENAI_BASE_URL=https://www.tken.shop/v1` visible if possible.
- `assets/preview-env-example.png`
  - Show `.env.example` with placeholder values only.

## Short GIF

- `assets/demo-openai-compatible-examples.gif`
  - Length: 8-12 seconds.
  - Copy `.env.example` to `.env`, set placeholder-safe values, then run one example.
  - Do not reveal a real API key.

## Alt Text

- Terminal preview: `OpenAI-compatible Node.js example calling the TKEN API gateway`
- GIF: `Running a TKEN OpenAI-compatible chat completion example from the terminal`

## CTA Rule

Every public asset should point readers back to:

https://www.tken.shop/
